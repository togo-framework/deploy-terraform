# deploy-terraform — docs

Terraform (IaC) deploy driver — runs terraform init/plan/apply (and destroy) in a Terraform directory.

Install: `togo install togo-framework/deploy-terraform` · Select with `DEPLOY_PROVIDER=terraform`.
